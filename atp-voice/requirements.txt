# atp-voice runtime dependencies (torch/torchaudio installed separately in
# the Dockerfile from the CPU-only PyTorch index)
fastapi>=0.115
uvicorn[standard]>=0.34
pg8000>=1.31
soundfile>=0.12
numpy>=1.26
requests>=2.32
speechbrain>=1.0.2
# optional A/B backend (CAM++ via FunASR):
funasr>=1.2
